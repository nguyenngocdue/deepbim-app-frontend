services:
  nginx-proxy:
    image: nginx:latest
    container_name: nginx_proxy
    restart: always
    expose:
      - "80"
      - "443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
      - ../.docker_configs/certs:/etc/nginx/certs  # Đảm bảo chứng chỉ được mount đúng
    networks:
      - custom_network
    depends_on:
      - frontend

  frontend:
    build:
      context: ..
      dockerfile: .docker_202/Dockerfile
    container_name: frontend_container_202
    restart: always
    expose:
      - "443"
    networks:
      - custom_network

networks:
  custom_network:
    driver: bridge
