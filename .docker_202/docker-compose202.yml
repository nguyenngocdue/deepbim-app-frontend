services:
  frontend:
    image: nguyenngocdue/vite-frontend:beta
    restart: always
    ports:
      - "8080:80"
      - "443:443"
    volumes:
      - /etc/letsencrypt:/etc/letsencrypt
      - /var/www/certbot:/var/www/certbot
      - ~/frontend-deploy/nginx.conf:/etc/nginx/conf.d/default.conf
