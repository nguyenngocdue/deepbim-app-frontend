server {
    listen 80; # Đổi từ 3003 sang 80
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html index.htm;

    location / {
        try_files $uri $uri/ /index.html; # Hỗ trợ SPA routing
    }

    access_log off;
    error_log /var/log/nginx/error.log;
}